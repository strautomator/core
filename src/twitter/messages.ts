// Strautomator Core: Twitter messages

/**
 * Long distance rides.
 */
export const RideLongDistance = ["A massive ${distance} ride by ${user}! Well done!", "An impressive ${distance} ride by ${user} just updated with Strautomator!", ""]

/**
 * Fast rides.
 */
export const RideFast = ["Impressively fast ride by ${user}! ${speedAvg} average speed 😲", "Rocket 🚀 Check that ${speedAvg} ride by ${user}."]

/**
 * Long distance runs.
 */
export const RunLongDistance = ["What a massive ${distance} run by ${user}!", "Impressive ${distance} run by ${user} just automated with Strautomator!", ""]
