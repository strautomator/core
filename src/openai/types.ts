// Strautomator Core: OpenAI types

/**
 * ChatGPT prompt and response data.
 */
export interface ChatGptResponse {
    /** Prompt sent to ChatFPT. */
    prompt: string
    /** Response from ChatGPT. */
    response: string
}
