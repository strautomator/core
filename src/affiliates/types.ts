// Strautomator Core: Affiliates types

/**
 * Affiliate product.
 */
export interface AffiliateLink {
    /** Link title. */
    title: string
    /** Link description. */
    description: string
    /** Publisher. */
    publisher: string
    /** Target URL. */
    url: string
}
